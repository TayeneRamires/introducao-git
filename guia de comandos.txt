git init -> criar repositório local

git remote add origin 'link do github' -> copiar isto do github

git add . -> criar os arquivos que eu preciso

git commit -m "mensagem de commit" -> adiciona a mensagem do commit que eu escrever

git push origin main -> enviar os arquivos